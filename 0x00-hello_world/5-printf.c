#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Message[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", Message);
	return (0);
}

