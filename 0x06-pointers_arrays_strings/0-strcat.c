#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: destination
 * @src: source
 * Return: char array
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
