#include "main.h"
#include <stdio.h>
#include <stdlid.h>

/**
 * _strstr - get the first occurence of the smaller string
 * @haystack: larger string
 * @needle: smaller string
 * Return: pointer to the positoin of the first occurence
 */
char *_strstr(char *haystack, char *needle)
{
	int i;
}
